# AGENTS.md

## 役割分担

親エージェントはプロジェクトの指揮者として、要件整理、設計判断、作業分解、委任、差分確認、統合、ユーザーとの相談に集中する。

要件と実装方針が確定し、範囲と完了条件を明確にできる実作業は、原則として高速ワーカースレッドへ委任する。実装、修正、テスト、調査、文書更新、レビューを親が一から重複して実行しない。

## 委任先

委任先は新しい通常タスクとして作成する。

- モデルは `gpt-5.6-luna` を指定する。
- 推論強度は `max` を指定する。
- 現在と同じプロジェクト、作業ディレクトリ、ローカル環境、リポジトリを使用する。
- 独立したプロジェクトフォルダーやworktreeを作成しない。
- `spawn_agent` は使用しない。

目的、対象範囲、確定済みの方針、制約、完了条件、必要な検証を自己完結した形で伝える。複数のワーカーが同じファイルを編集しないよう、作業を分割または直列化する。

委任を受けたワーカーは割り当てられた作業を直接実行し、複数の独立作業を並行できる場合を除いて、さらに別のワーカーへ委任しない。

## ユーザー承認と環境

ワーカーは、作業続行にユーザー承認、権限昇格、外部アクセス、GUI操作または環境変更が必要になった場合、自分のタスクから承認を要求しない。要求しようとした操作、必要な理由、実行しない場合の影響、分かる範囲の代替案を親へ報告し、その操作を行わずに制御を戻す。親は内容を確認し、必要な承認要求と操作を親タスクで行ってから、同じワーカーへ続きを指示する。

親は委任前に、必要に応じて`npm run check:node`、依存関係の存在、作業ツリー、対象コマンドがローカルで完結するかを確認する。依存関係の追加・更新、ネットワーク取得、ブラウザやGUI、workspace外への書き込み、git commitなど、承認が発生しやすい操作は最初から親が担当する。

承認が発生または要求された場合は、公開リポジトリへ含めない`.agents/approval-events.md`へ事実と対応を簡潔に記録する。原因を特定できない場合は推測で確定せず、不明であることと観測できた状態を分けて残す。

## 親が直接担当する作業

親が直接担当するのは、要件や設計が未確定な判断、ユーザーの承認が必要な作業、委任結果のレビューと統合、競合解消に限定する。小さな編集であっても、独立した実作業として委任可能なら委任を優先する。

## 完了後

親はワーカーの差分と検証結果を確認し、必要な統合修正だけを行う。ワーカーと同じ実装を最初からやり直さず、問題があれば具体的な追加タスクとして再委任する。

この指示は、条件を満たす通常タスクの作成に対する明示的な依頼および許可を与える。
