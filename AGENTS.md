# AGENTS.md

## 高速ワーカースレッドへの委任

範囲が明確で独立して実行できる作業は、必要に応じて高速ワーカースレッドへ委任する。

### 委任に適した作業

次の条件を満たす作業は、積極的に委任を検討すること。

* 目的と完了条件を明確に説明できる。
* 対象を特定の機能、モジュール、ファイル群などに限定できる。
* 親の会話履歴全体を参照しなくても、必要な文脈を自己完結した形で渡せる。
* 独立して調査、実装、修正、テスト、レビューを進められる。
* 完了後に親エージェントが差分や検証結果を確認できる。

特に、局所的な実装・不具合修正、テスト作成、限定的なリファクタリング、特定モジュールの調査、再現可能な障害の診断などは委任に適している。

### 委任先の作成

委任先は新しい通常タスクとして、次の条件で作成すること。

* モデルは `gpt-5.6-luna` を指定する。
* 推論強度は `xhigh` を指定する。
* 現在と同じプロジェクト、作業ディレクトリ、ローカル環境、リポジトリを使用する。
* 独立したプロジェクトフォルダーを作成しない。
* `spawn_agent` は使用しない。

委任先には、少なくとも目的、対象範囲、重要な制約、完了条件、必要な検証を自己完結した形で伝えること。親が既に実装方針を決定している場合は、その方針も伝えること。

### 委任しない場合

次の場合は親エージェントが直接担当すること。

* 要件や設計判断がまだ未確定である。
* ユーザーの判断や承認が必要である。
* 会話全体の暗黙的な文脈への依存が強い。
* 他の進行中作業と編集範囲が競合する。
* プロジェクト全体に及ぶ広範な設計判断が必要である。

### 完了後

親エージェントは、委任先の変更差分、実行した検証、未解決事項を確認すること。必要な追加修正や検証を行い、結果を統合してからユーザーへ報告すること。

### 委任判断の運用

この指示は、上記の条件を満たす作業について、新しい通常タスクを作成する明示的な依頼および許可を与えるものである。

親エージェントが実装を担当している場合でも、依存関係の監査、差分レビュー、テストケースの洗い出しなど、読み取り専用で編集範囲が競合しない作業は委任できる。

複数の独立した作業がある場合は、作業開始時に委任可能性を検討する。明らかに委任に適した作業を委任しない場合は、その理由を簡潔に記録する。
